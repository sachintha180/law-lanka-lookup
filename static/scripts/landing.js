import {
  enableLoginClick,
  enableLoginClick,
  enableRegisterClick,
} from "./common.js";

window.onload = init;

function init() {
  // Enable the logo click
  enableLogoClick();

  // Enable the login button click
  enableLoginClick();

  // Enable the register button click
  enableRegisterClick();
}
